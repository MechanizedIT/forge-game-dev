# Fallback Implementation Prompt — Prepared Generated Quest Proof

Use this only if the primary Task A cannot reach a real generated completion by the end of the first major implementation day. It proves the most valuable boundary with less generality during Day 2. Do not execute it in the planning session.

---

Implement a reduced Forge alpha proof: complete one **prepared generated quest** in the existing clean Gravity Tap Arena project through bounded Codex work, deterministic proof, real creator play, exact completion, local Git, and restart. Do not build general generated-quest adjustment, general verification-profile selection, editor launch, Task B, or any other capability.

## Checkpoint and repository rules

- Start from `main` `1e734bf060ff3d17abcd45678a95e03d3690ba46` plus the reviewed tip commit of `planning/alpha-proof-game` containing this prompt; resolve and record both SHAs.
- Create a separate worktree and `codex/` implementation branch. Verify `v0.2.0` still points to `08cffa71cd802b14c6c72ad343f9fa5b4007a482`.
- Read root/nested `AGENTS.md`, repository guide/change map, alpha architecture, roadmap, proof game, and primary prompt. Protect the sample, creation path, other generated projects, showcase, evidence, and tag.
- Write a reduced plan and stop for approval before implementation.

## Exact scope

Target only registered `gravity-tap-arena-6cbe7b2a54` at clean baseline commit `7dbbbf43f206cd5334b226d6c9a98fbfcf0e10a8`. If that project/path/commit is unavailable or dirty, stop and report the blocker; do not scan for or substitute another project.

Prepare one Forge-owned, versioned revision for its first available quest, then show and require explicit creator acceptance of that revision before preparing the contract. Preserve the original generated quest/blueprint as provenance. The accepted revised result is:

> The existing objective placeholder moves/reframes as one clearly identifiable gravity orb near the arena's center, with a stable observable identity that later pull behavior can target.

This is a real visible delta from the current starter, not a claim that Q1's original broad criteria were already satisfied. The prepared proof adapter may support only this exact owner-approved revision; do not pretend it is general adjustment.

Allow only the minimum existing files from `scenes/main.tscn`, `scripts/main.gd`, and `scripts/objective_marker.gd` that the inspected project actually needs. Maximum three. No new/delete/rename. No Codex edits to `.forge`, `.git`, `.godot`, verifier, cache, dependency, external asset, executable/shell file, symlink, or another project. Forge assigns every path, command, model/runtime value, timestamp, run ID, state, verification, and Git action.

## Minimal architecture

Add a generated-quest-runner sibling with only the pieces required for this prepared contract:

- strict prepared contract and fingerprint;
- project run lock/journal and clean-HEAD/inventory/prehash gates;
- official SDK workspace-write/no-network invocation with focused ≤40,000-character context;
- understandable progress and sanitized evidence;
- exact diff boundary review;
- pinned Godot baseline/project-health proof;
- one Forge-owned `gravity_orb_presence_v1` mechanic verifier outside editable paths;
- Play and creator choices Worked / Did not work / Not ready / Retry / Cancel;
- exact run-owned rollback with current-posthash comparison and concurrent-edit refusal;
- atomic quest/roadmap/Chronicle/docs/project/provenance completion, exact staging, one commit with run ID, ignored actual-SHA receipt, fresh-service reload.

The Project World remains a read snapshot; the sample runner remains unchanged. Host routes may be specific to prepare/start/status/play/confirm/rollback this one prepared generated contract, but they must accept only project ID/quest ID and exact bodies, enforce same origin, and never accept paths/commands/configuration from the browser.

## Completion rules

Only creator `Worked`, after final boundary/project/mechanic proof, may complete. Update the selected quest, roadmap/unlocks, Chronicle `quest_completed`, deterministic Markdown, project state/next recommendation, and implementation provenance in one transaction. Commit source and tracked artifacts once using `forge: complete <questId> [run:<runId>]`; then store actual SHA in ignored `.forge/local/runs/<runId>/commit.json`. On commit failure restore transaction-owned state/doc preimages and retain the reviewed source diff for Retry/Roll back. Never use broad deletion, `git clean`, or hard reset.

## Required evidence

- Malicious/path/schema/dirty-worktree tests and exact failure behavior.
- Unapproved change/new/delete/concurrent edit rejection.
- All confirmation outcomes and rollback refusal/success.
- Transaction failure injection and commit/receipt restart recovery.
- Protected sample/creation/world regressions, `npm run context:check`, `npm run check`, `npm run check:v0.1`, build, and `git diff --check`.
- Browser review of prepared contract, progress, proof, play, confirm, completion, recovery on desktop/tablet/mobile with accessibility/reduced-motion/console checks.
- One real official-SDK run, visible creator play and non-fabricated UI confirmation, completion commit, and fresh-service restart on Gravity Tap.
- Final initial/final inventories and proof that other generated projects, sample, showcase, tag, and release are unchanged.

## Explicit cuts

No generic quest adjustment or arbitrary generated quest eligibility. No new files. No supported-foundation/intake/roadmap redesign. No idea promotion, Companion chat, editor action, Web export, publishing, showcase refresh, or second quest. Do not represent this prepared adapter as a general external-test alpha; label it a Build Week proof of the generated-project transaction.

Update milestone closeout/status/roadmap/AI work log only after real evidence passes. Stop after one clean implementation commit and report the exact residual limitation.

---
