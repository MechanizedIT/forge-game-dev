# Dashboard Judge-Path Reliability Plan

- **Workflow state:** `COMPLETE`
- **Baseline:** `b89814922bd41923dbe214c89947aa46b28002ba`
- **Scope:** Diagnose the failed live implementation and make the smallest correction needed for one real human-confirmed dashboard run.

## Root-cause test

Compare the failed three-file diff and verification evidence with the prepared quest, plan, verifier, and earlier successful CLI implementation. Change no product behavior until the failing layer is identified.

## Approved correction

If the failure is prompt reliability, add only a concrete Godot node-binding constraint to the bounded work packet. Preserve the three writable gameplay files, SDK sandbox, acceptance criteria, verification commands, fixture, reset path, CLI path, and dashboard gates.

## Acceptance criteria

- A focused test proves the bounded packet requires an exported `NodePath` resolved after the scene enters the tree.
- All offline tests and the dashboard build pass.
- A reset workspace completes one real dashboard SDK run with exactly the three approved files changed.
- `VERIFY-1` and `VERIFY-2` pass without weakened checks.
- Godot launches, the creator supplies a genuine result, and only exact success persists `PASS`, workflow `COMPLETE`, and roadmap `completed`.
- Sanitized evidence and concise closeout documents record both the failure and the successful correction.
