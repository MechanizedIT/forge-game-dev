# v0.2 Task 1 Baseline Protection Review

- **Workflow state:** `REVIEW`
- **Verdict:** `PASS`

## Acceptance review

1. **PASS — Release references:** Annotated tag `v0.1.0` still resolves to `99a439aff4425cc3572bd90eb2412ea89ff052ad`; refined fallback `4d276125aeb44d007cca96193daf9515fca2a99e` matches `origin/main`.
2. **PASS — Development boundary:** `feature/v0.2-living-workshop` was created from the refined fallback while preserving the approved untracked plan.
3. **PASS — Legacy launch:** `npm run forge:v0.1` builds both entries and opens `/legacy.html`; an HTTP smoke returned 200.
4. **PASS — Normal launch unchanged:** The `npm run forge` script remains `npm run dashboard:build && npm run dashboard:host`.
5. **PASS — Protected UI:** `LegacySampleApp.tsx` has the same Git object hash as the pre-task `App.tsx`; the default entry re-exports that component.
6. **PASS — Focused compatibility:** `npm run check:v0.1` passed 37/37 tests covering prepared artifacts, approval, duplicate-run rejection, ordered progress, verification/scope gates, launch/confirmation behavior, persisted completion, reset, and CLI cancellation.
7. **PASS — Full regression:** `npm run check` passed typechecking and 49/49 tests.
8. **PASS — Real Godot evidence:** Godot 4.7 emitted `FORGE_ENEMY_TARGETING_VERIFY_OK idle=pass detection=pass chase=pass player=pass controls=arrows+wasd`.
9. **PASS — Scope preservation:** No file changed under the fixture, quest runner, Godot runtime, demo workspace implementation, prepared quest loader, contracts, verification, reset, or completion services.
10. **PASS — Planning correction:** Required v0.2 scope is one Top-down arena starter; additional starters, generated implementation, and sample art are optional after required rehearsal.

## Scope review

No Launchpad, visual redesign, GPT planning, blueprint contract, starter project, or new-project persistence was implemented. The host change only selects the dedicated legacy URL when explicitly launched with `--legacy`.
