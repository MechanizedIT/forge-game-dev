# Forge v0.2 Task 5 Review — Controlled Project Creation

- **Workflow state:** `COMPLETE`
- **Verdict:** `PASS`

## Acceptance review

1. **PASS — Approval and write boundary:** Blueprint Ready shows the exact project, Top-down Arena foundation, quest count, Forge-owned destination, Godot/GDScript boundary, no external dependencies, Godot check, and local Git baseline. A separate final confirmation is required. The creation endpoint rejects missing origin, missing/replayed nonce, wrong confirmation, or missing approved blueprint before starting the service.
2. **PASS — Truthful progress:** The UI renders the seven service-owned stages in order, real project name/foundation/server-owned identifier, real elapsed time, current explanation, and assembling/verifying Companion states without percentages, estimates, invented counts, model commands, or simulated output.
3. **PASS — Controlled starter:** The committed `1.0.0` starter contains exactly seven inventoried files, a launchable scene, `CharacterBody2D` player, WASD/arrow movement, bounded arena, visible objective marker, title/objective copy, camera-safe framing, and code-native visuals. It contains no external art, audio, font, dependency, plugin, download, or generated package.
4. **PASS — Destination safety:** Project IDs are Forge-owned, sanitized, length-bounded, Windows-reserved-name-safe, collision-safe, and direct canonical descendants of `<Forge home>/projects`. Tests cover invalid/traversal-like names, reserved names, occupied destinations, unowned cleanup, and junction/symlink escape.
5. **PASS — Controlled transaction:** Forge revalidates and fingerprints the approved blueprint, creates a known staging directory, copies only the manifest inventory, applies only the display-name token, writes/reloads artifacts, verifies Godot, creates Git, writes local evidence, atomically promotes, and registers last.
6. **PASS — Artifact ownership:** Strict schemas cover the manifest, approved blueprint, vision, first playable, roadmap, individual quests, project state, Chronicle, planning provenance, starter manifest, Godot result, Git result, creation provenance, failure evidence, and registry. Markdown is deterministic from JSON. Portable artifacts reject absolute local paths. Roadmap owns quest state; project state owns view/selection/last-opened; Chronicle records creation only.
7. **PASS — Godot verification:** The fixed Forge-owned invocation uses `--headless --path <staging> --script res://scripts/verify_project.gd`. The verifier checks the configured main scene, required nodes, GDScript parsing, input actions, executable movement via arrow and WASD input, objective marker, camera, successful exit, and absence of fatal resource/script output. The real run used pinned Godot `4.7.stable.official.5b4e0cb0f` and emitted `FORGE_TOP_DOWN_ARENA_VERIFY_OK`.
8. **PASS — Git baseline:** Forge initializes local Git, configures project-local identity, stages exactly the expected portable tree, creates `Forge project baseline`, verifies `HEAD`, proves an empty porcelain status, and proves zero remotes. Post-commit Godot/Git/creation records live under ignored `.forge/local/` so the one baseline commit can record its own SHA without creating a self-referential dirty commit.
9. **PASS — Failure and cancellation:** Godot failure, Git failure, cancellation, occupied destination, and stale blueprint tests leave no registry entry, remove only transaction-owned verified paths, preserve concise external failure evidence, and never call sample reset. A late post-registration failure removes the matching registry entry before transaction-owned cleanup.
10. **PASS — Registry and restart:** The atomic strict registry rejects duplicate IDs, preserves malformed input as a backup on recovery, keeps missing project entries without deleting projects, and discovers the real project after a fresh service instance. Launchpad shows `Created · Project World integration pending`; reopen returns the actual Project Created summary.
11. **PASS — Browser review:** Edge captures final confirmation, progress/Godot stage, Project Created at desktop/tablet/mobile, reduced motion, Launchpad recent, reopened summary, and controlled failure. The report contains zero console, exception, same-origin request, overflow, action, overlap, focus, or reduced-motion issues.
12. **PASS — Real rehearsal:** Real GPT-5.6 high produced a valid four-quest **Last-Moment Pulse** blueprint in one response. Forge created `last-moment-pulse-6631032087`, saved 32 relative files, passed Godot, created Git baseline `9f73f5040bac9b67e806a56129170a150c139637`, reloaded the registry, reopened the summary, and launched the real project for 120 visible frames.
13. **PASS — Regression and scope:** `npm run check` passed 79/79; `npm run check:v0.1` passed its production build and 37/37 protected tests. The sample workspace hash did not change. No sample fixture, prepared quest, runner, verifier, completion, reset, default launch, legacy launch, generated Project World, generated-quest execution, additional starter, import path, or sample art was added or altered.

## Verification

- `npm run check`: type checks plus 79/79 tests passed.
- `npm run check:v0.1`: production build plus 37/37 passed.
- `npm run visual:review:v0.2:creation`: 10 captures, zero issues.
- `npm run project:create:rehearse`: real GPT/Godot/Git/restart/launch `PASS`.
- Direct controlled starter Godot verification: stable success marker emitted.
- Generated project Git status: clean; remote list empty.
- `git diff --check`: passed.

## Deferred boundary

Task 6 owns generated Project World. Generated-quest implementation, additional starters, existing-project import, and sample visual-art changes remain deferred.
