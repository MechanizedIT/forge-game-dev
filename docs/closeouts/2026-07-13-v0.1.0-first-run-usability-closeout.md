# v0.1.0 First-Run Usability Closeout

- **Workflow state:** `COMPLETE`
- **Result:** Focused first-time judge refinement implemented and verified with one documented responsive-review condition

## Direct response to user testing

Forge now introduces itself before presenting project detail, makes the starting action unmistakable, explains preserved progress and reset consequences, keeps unavailable areas disabled, stays visibly active during long Codex work, and gives exact arrow/WASD play instructions.

The dashboard reset delegates to the original safe reset path. The SDK flow, three-file gameplay boundary, deterministic review, human confirmation gate, persistence owner, CLI, and immutable fixture-copy behavior are unchanged.

## Live closeout

The first live refinement run exposed a Godot 4.7 parse error caused by Variant-derived verifier inference. The baseline and bounded packet now require typed `CharacterBody2D` nodes. A clean reset and second real SDK run passed all automated checks, stayed within scope, launched Godot, and stopped incomplete after confirmation was cancelled.

## Next action

Perform one manual narrow-window visual pass, then return to the already-planned submission packaging work. Do not add product scope.
