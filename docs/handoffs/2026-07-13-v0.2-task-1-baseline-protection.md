# v0.2 Task 1 Baseline Protection Handoff

- **Workflow state:** `DOCUMENT`
- **Branch:** `feature/v0.2-living-workshop`
- **Protected release:** `v0.1.0` → `99a439aff4425cc3572bd90eb2412ea89ff052ad`
- **Refined fallback:** `4d276125aeb44d007cca96193daf9515fca2a99e`

## TL;DR

The existing sample experience now has an explicit legacy entry and focused compatibility command. The original sample component is byte-identical, the normal Forge command is unchanged, and protected runtime paths have no diff.

## Current evidence

- `npm run check:v0.1`: 37/37 passed, including production build.
- `npm run check`: 49/49 passed.
- `npm run godot:verify`: Enemy Targeting success token passed on Godot 4.7.
- `/legacy.html`: HTTP 200 through the real local host.
- Protected App Git object hash before and after extraction: `6bffe9d75cf0f703d61e970304fd6f0fba2c6c1c`.
- Protected fixture, runner, Godot, workspace, quest-loader, contract, reset, and completion paths: no diff.

## Remaining risks

- Future visual work must not overwrite the legacy component or shared styling without first keeping a stable legacy stylesheet.
- The legacy entry uses the same protected backend by design; future service changes still require `npm run check:v0.1` before closeout.
- A full live Codex/play confirmation run was not repeated because Task 1 changed no runtime behavior; existing clean-machine and live-run evidence remains authoritative.

## Next bounded action

Build only the Launchpad and Project World shell with fixture data. Keep default Forge and legacy mode on the protected sample backend. Do not add GPT planning, starter creation, persistence, or sample art yet.
