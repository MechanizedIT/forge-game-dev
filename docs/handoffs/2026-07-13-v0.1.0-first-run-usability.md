# v0.1.0 First-Run Usability Handoff

- **Workflow state:** `DOCUMENT`
- **Result:** The real judge path is clearer without a second workflow or state system.

## Implemented

- Added a one-sentence Forge introduction, five-step demo path, explicit three-file/proof explanation, and dominant **Review Enemy Targeting** start action.
- Made preserved workspace state explicit in both setup output and the dashboard, including the exact terminal reset command.
- Added dashboard **Start fresh demo** confirmation that calls the existing safe reset function; cancellation preserves progress.
- Disabled Proof until review evidence exists and Chronicle until completion exists.
- Reworked the active run into a dominant progress surface with current and completed stages, elapsed time, reassurance, and optional technical evidence.
- Added arrow-key and WASD support to the baseline, deterministic Godot checks for both, and four exact pre-launch instructions.
- Added one narrow GDScript typing constraint after the first live refinement run exposed a real verifier parse failure; the clean rerun passed without weakening verification.

## Evidence

- Offline typechecking and 48 focused tests pass.
- Godot 4.7 verifies the fresh baseline with `controls=arrows+wasd`.
- A real SDK rerun changed only the three approved files and returned `CONDITIONAL PASS` with AC-1 through AC-5 passed.
- Godot launched and closed cleanly; confirmation was deliberately cancelled because no new human visual result was supplied.
- Sanitized live evidence: [`../evidence/2026-07-13-v0.1.0-first-run-usability.json`](../evidence/2026-07-13-v0.1.0-first-run-usability.json).

## Remaining risk

The live browser review covered the real desktop state. The available in-app browser surface did not expose viewport resizing, so one manual narrow-window visual pass remains for submission packaging; responsive rules and narrow layouts were reviewed in code and production build output.
