# Forge v0.2 Task 8 Showcase Edge Review

Microsoft Edge and the pinned Playwright `1.61.1` review the static production build at `1440×900`, `768×900`, and `390×844`, plus a reduced-motion mobile deep link.

The harness checks the hero and primary actions, page-level horizontal overflow, walkthrough path selection, previous/next navigation, keyboard arrows, focus movement to the changed heading, touch/click controls, query-string deep links, the truthful unconfigured video dialog, Escape close, focus return, console errors, page exceptions, same-origin request failures, and reduced motion.

The machine-readable result is in `review.json`. Screenshots are derived Task 8 review records; they are not operational Forge evidence.
